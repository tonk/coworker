meta {
  name: List Linked Board Cards
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/customers/{{customerId}}/tickets/{{ticketId}}/cards
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
