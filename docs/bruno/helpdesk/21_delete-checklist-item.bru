meta {
  name: Delete Checklist Item
  type: http
  seq: 21
}

delete {
  url: {{baseUrl}}/api/v1/customers/{{customerId}}/tickets/{{ticketId}}/checklist/{{itemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
