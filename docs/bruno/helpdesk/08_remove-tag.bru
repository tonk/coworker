meta {
  name: Remove Tag
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/customers/{{customerId}}/tickets/{{ticketId}}/tags/{{tagId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
