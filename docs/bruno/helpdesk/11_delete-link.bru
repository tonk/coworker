meta {
  name: Remove Ticket Link
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/api/v1/customers/{{customerId}}/tickets/{{ticketId}}/links/{{linkId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
