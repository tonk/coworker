meta {
  name: Delete Ticket
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/customers/{{customerId}}/tickets/{{ticketId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
