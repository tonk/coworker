meta {
  name: Delete Checklist Template (Admin)
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/api/v1/admin/ticket-checklist-templates/{{templateId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
