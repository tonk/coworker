meta {
  name: Delete Inbox Checklist Item
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/api/v1/tickets/inbox/{{ticketId}}/checklist/{{itemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
